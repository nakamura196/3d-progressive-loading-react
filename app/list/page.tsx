'use client';

import { Layout } from '../../src/components/Layout';
import { ModelsListWrapper } from '../../src/components/ModelsListWrapper';

export default function ListPage() {
  return (
    <Layout>
      <ModelsListWrapper />
    </Layout>
  );
}